import styled from 'styled-components';

export default styled.div`
  img {
    max-width: 80%;
    max-height: 600px;
    margin-top: 100px;
    border-radius: 5px;
  }

  p {
    color: white;
    border-radius: 5px;
    background: rgba(0, 0, 0, 0.5);
    width: fit-content;
    padding: 10px 20px;
    margin: auto;
  }
`;
